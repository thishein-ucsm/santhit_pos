to export py to exe
pyinstaller --onefile splash.py --windowed

to create requirements.txt
pip freeze > requirements.txt

to install requirements.txt
pip install -r requirements.txt

to remove unused library
pip uninstall unused-library

copy and paste assets files to c:\tetlu\

neet to add sqldump path to system environment variable